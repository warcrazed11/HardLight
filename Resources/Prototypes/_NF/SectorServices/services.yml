- type: sectorService
  id: PirateBounties
  components:
  - type: SectorPirateBountyDatabase

- type: sectorService
  id: Mail
  components:
  - type: SectorLogisticStats
  - type: SectorMail # Contains sector-wide mail parameters

- type: sectorService
  id: ShuttleRecords
  components:
  - type: SectorShuttleRecords

- type: sectorService
  id: BankAccounts
  components:
  - type: SectorBank
    accounts:
      Engineering:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 40 # 72K per hour, about enough for minimum wage for the default NFSD size
      Science:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 12 # 21.6K per hour, a bit over for the DOC MOM's wage.
      Service:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 32 # TODO: move to ~65K per hour (~18/s), about enough for minimum wage for Frontier employees
      Security:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 60 # 72K per hour, about enough for minimum wage for the default NFSD size
      Cargo:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 12 # 21.6K per hour, a bit over for the DOC MOM's wage.
      Frontier:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 36 # TODO: move to ~65K per hour (~18/s), about enough for minimum wage for Frontier employees
      Nfsd:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 60 # 72K per hour, about enough for minimum wage for the default ColSec size
      Medical:
        !type:SectorBankAccountInfo
        balance: 8000
        increasePerSecond: 12 # 21.6K per hour, a bit over for the DOC MOM's wage.

 # A global source of character records (DNA, fingerprints, criminal records, etc.)
- type: sectorService
  id: SectorRecords
  components:
  - type: StationRecords

- type: sectorService
  id: Alerts
  components:
  - type: AlertLevel
    alertLevelPrototype: stationAlerts

- type: sectorService
  id: BountyContracts
  components:
  - type: BountyContractData

- type: sectorService
  id: PublicTransit
  components:
  - type: SectorPublicTransit
